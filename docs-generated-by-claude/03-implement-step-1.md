# 03 — Implementing Step 1

Generated from `prompts/03-implement-step-1.md`. Implements Step 1 of [`02-implementation-plan-step-by-step.md`](02-implementation-plan-step-by-step.md) — *Repo skeleton & `uv` project*.

## Verifications

All four checks from the plan's Step-1 verify clause pass:

```
$ uv sync                                # → installs Python 3.11.15 + ruff 0.15.12
$ uv run python -m harness --help        # → prints usage line ✓
$ uv run python -m harness --version     # → "harness 0.0.1" ✓
$ uv run ruff check .                    # → All checks passed ✓
$ uv run ruff format --check .           # → 2 files already formatted ✓
```

## Files created

- `pyproject.toml` — package `coding-agent-compare` 0.0.1, Python ≥ 3.11, hatchling build backend, ruff in dev group
- `.python-version` — `3.11`
- `harness/__init__.py` — declares `__version__`
- `harness/__main__.py` — argparse stub; `--help` and `--version` work, no subcommands yet
- `ruff.toml` — `target-version = "py311"`, `line-length = 100`, defaults otherwise
- `tasks/.gitkeep`, `runs/.gitkeep` — placeholders so the dirs exist at clone time
- `.gitignore` — updated to ignore `.venv/`, `__pycache__/`, `.uv-cache/`, `*.egg-info/`, `*.pyc`, and `runs/*` (with `!runs/.gitkeep` exception)

## Files NOT created (deliberate)

- No new entries under `scripts/` — that dir already exists with the two checkers.
- `.venv/` and `uv.lock` were generated by `uv sync` but only `uv.lock` should be tracked. `.venv/` is correctly gitignored.

## Incidentals worth flagging

- `uv.lock` is now on disk. Convention is to **commit it** for reproducibility (run manifests in later steps will reference exact versions). It should land in the Step-1 commit.
- The CLI currently prints help when invoked with no args. When subcommands land (Step 3+), this will switch to argparse's standard "print help if no subcommand" pattern.
